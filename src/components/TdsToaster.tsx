"use client";

import { Toaster } from "@tatum-io/tatum-design-system";

/** Client-only host for TDS toast portal (layout is a Server Component). */
export function TdsToaster() {
  return <Toaster />;
}
