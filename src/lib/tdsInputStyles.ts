/**
 * Inner `<input>` classes for TDS `Input` on dark surfaces:
 * no dark fill — white field — black, bold value text.
 */
export const TDS_GAME_INPUT_CLASSNAME =
  "bg-tatum-white text-tatum-gray-900 font-tatum-bold placeholder:font-tatum-regular placeholder:text-tatum-gray-500";
